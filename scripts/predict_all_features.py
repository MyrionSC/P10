from DNNRegressor import predict
from batch_configs.supersegment_features_grid import configs

for conf in configs:
	first = True
	if "Iter_1" in conf['model_name_base']
		predict(conf, True, first)
		firts = False